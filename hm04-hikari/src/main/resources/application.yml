spring:
  datasource:
    url: jdbc:postgresql://192.168.1.90:5432/task4db
    username: postgres
    password: masterkey

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:bd/migration